


#
# Get needed base tools in Debian
#
RUN apt-get update && apt-get install -y \
        ca-certificates \
        git             \
        openssh-client  \
        wget            \
        unzip           \
    && rm -rf /var/lib/apt/lists/*
